apiVersion: v1
kind: ServiceAccount
metadata:
  name: iris-ml-sa
  namespace: iris-ml
  labels:
    app.kubernetes.io/name: iris-ml
  annotations:
    # Azure Workload Identity annotation (add when configured)
    # azure.workload.identity/client-id: "<client-id>"
